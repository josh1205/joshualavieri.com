# CLAUDE.md

Read **[AGENTS.md](AGENTS.md)** — it holds this repository's setup, house rules,
and index of skills, and is kept vendor-neutral so every tool reads the same
instructions.

Skills live in `.agents/skills/`. `.claude/skills` is a symlink to that
directory so they are discovered normally; there is nothing Claude-specific in
them.
